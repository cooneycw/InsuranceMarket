## start redis
## ________________________________________________________________________________
## sudo snap install docker
## sudo groupadd docker
## sudo usermod -aG docker $USER
## sudo chown $USER /var/run/docker.sock
## docker run -d -p 6379:6379 --name redis_instance --restart unless-stopped redis


# to - do (important)
# create dividend / p.v. firm page
# add trend report to decision page
# create decision page (add multi-logic)

# to - do (backlog)
# add default style for player (in event they time-out in multi-player)
# validate multi-player logic
# claim report showing development triangle by coverage
# time series industry trend (create vector of trend history in config)
# extend bi development to 5 years